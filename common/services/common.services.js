(function() {
	"use strict";

	// This is the main module provides interaction with the web server
	// the $ngResource is the dependency for communication with web services
	// and this is done by the $resource module which is in the ngResource
	angular
		.module("common.services", ["ngResource"])
}());